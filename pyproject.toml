[project]
name = "domain-checker"
version = "0.1.0"
description = "Rapidly check domain availability across all gTLDs"
requires-python = ">=3.10"
dependencies = [
    "dnspython>=2.6",
    "asyncwhois>=1.1",
]

[project.scripts]
domain-checker = "checker.cli:main"
